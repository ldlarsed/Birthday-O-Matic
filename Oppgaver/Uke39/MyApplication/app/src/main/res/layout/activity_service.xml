<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".ServiceActivity">

    <Button android:text="Start Service"
        android:id="@+id/start"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/broadcast"
        android:layout_centerHorizontal="true"
        android:onClick="startService"></Button>

    <Button android:text="Start Broadcast Service"
        android:id="@+id/broadcast"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_centerHorizontal="true"
        android:onClick="startBroadcast"></Button>

    <Button
        android:text="Stop Broadcast Service"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/stopBroadcast"
        android:layout_below="@+id/broadcast"
        android:layout_centerHorizontal="true"
        android:onClick="stopBroadcast"></Button>

</RelativeLayout>
