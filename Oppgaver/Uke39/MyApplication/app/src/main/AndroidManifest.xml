<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.s198569.service" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ServiceActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Resultat"
            android:label="Resultat">
        </activity>

        <service android:name="MinService"></service>
        <service android:name="SettPeriodiskService"></service>

        <receiver android:name="MinBroadcastReceiver">
            <intent-filter>
                <action android:name="com.example.s198569.service.mittbroadcast" />
            </intent-filter>
        </receiver>
    </application>


</manifest>
