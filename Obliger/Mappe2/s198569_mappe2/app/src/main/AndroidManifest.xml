<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.s198569_mappe2" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterPerson"
            android:label="@string/title_activity_register_person" >
        </activity>
        <activity android:name=".RegisterMessage" >
        </activity>
        <activity
            android:name=".BuddyList"
            android:label="@string/title_activity_buddy_list"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>

        <service
            android:name=".services.BDayService"
            android:icon="@android:drawable/ic_popup_reminder"
            android:label="bday_service" />
        
        <receiver android:name=".broadcasts.BDayReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver android:name=".broadcasts.BDayStartServiceReceiver"></receiver>

    </application>

</manifest>
